# Spring Redis Demo

Starting redis:

```shell
docker-compose up
```

Executing:

```shell
curl -X POST -H 'Content-Type: application/json' \
-d '{"text": "Hello World!"}'
```

Instalando Artillery:

npm install -g artillery